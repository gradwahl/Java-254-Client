Clean plane-separated RuneScape build 254 terrain tiles.

Folders:
  plane_0/ plane_1/ plane_2/ plane_3/

File names are regionX_regionY.png. Each image is 64x64 pixels, one pixel per game tile.
These have no coordinate labels, no debug outlines, and no grid overlay.

Important: this export renders terrain/floor colours only. It does not render object models, icons, roofs, or walls.
Use image-rendering: pixelated / imageSmoothingEnabled=false when zooming in.

Exported regions: 452
Missing/unreadable regions: 5
